# 测试管理系统详细设计文档

## 1. 文档信息

| 文档名称 | 测试管理系统详细设计说明书 |
| :------ | :----------------------- |
| 版本号  | V1.0                     |
| 状态    | 草稿                     |
| 日期    | 2025-05-07               |

## 2. 系统架构设计

### 2.1 总体架构

测试管理系统采用前后端分离的微服务架构，主要分为以下几层：

1. **前端层**：提供用户界面，负责数据展示和用户交互
2. **API网关层**：负责请求路由、负载均衡、认证授权、限流熔断等
3. **微服务层**：核心业务逻辑实现，包括多个独立的微服务
4. **数据持久层**：负责数据存储和访问
5. **基础设施层**：提供监控、日志、消息队列等基础服务

系统架构图如下：

```
┌─────────────────────────────────────────────────────────────┐
│                        前端应用                              │
└───────────────────────────┬─────────────────────────────────┘
                            │
┌───────────────────────────▼─────────────────────────────────┐
│                         API网关                              │
└───┬───────────────┬───────────────────┬────────────┬────────┘
    │               │                   │            │
┌───▼───┐       ┌───▼───┐           ┌───▼───┐    ┌───▼───┐
│用例管理│       │计划管理│           │缺陷管理│    │报告管理│
│微服务 │       │微服务 │           │微服务 │    │微服务 │
└───┬───┘       └───┬───┘           └───┬───┘    └───┬───┘
    │               │                   │            │
┌───▼───────────────▼───────────────────▼────────────▼───────┐
│                      数据持久层                             │
└───────────────────────────┬─────────────────────────────────┘
                            │
┌───────────────────────────▼─────────────────────────────────┐
│                      基础设施层                             │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 系统模块划分

系统主要划分为以下核心模块：

1. **用户认证与权限管理模块**：负责用户登录、认证、授权和权限控制
2. **用例库管理模块**：负责测试用例库的创建、配置和管理
3. **测试用例管理模块**：负责测试用例的创建、编辑、组织和管理
4. **测试计划管理模块**：负责测试计划的创建、执行和监控
5. **缺陷管理模块**：负责缺陷的提交、跟踪和管理
6. **测试报告模块**：负责测试报告的生成、编辑和导出
7. **系统集成模块**：负责与外部系统的集成和数据交换

## 3. 技术栈选择

### 3.1 前端技术栈

| 技术/框架 | 版本 | 说明 |
| :------- | :--- | :--- |
| React | 18.2.0 | 前端UI框架，用于构建用户界面 |
| TypeScript | 5.0.4 | 静态类型检查的JavaScript超集 |
| Ant Design | 5.4.6 | 企业级UI组件库 |
| Redux | 4.2.1 | 状态管理库 |
| Redux Toolkit | 1.9.5 | Redux官方推荐的工具集 |
| React Router | 6.10.0 | 路由管理 |
| Axios | 1.3.6 | HTTP客户端 |
| ECharts | 5.4.2 | 数据可视化图表库 |
| Monaco Editor | 0.37.1 | 代码编辑器，用于编写测试脚本 |
| Jest | 29.5.0 | 前端测试框架 |
| Webpack | 5.80.0 | 模块打包工具 |
| ESLint | 8.39.0 | 代码质量检查工具 |
| Prettier | 2.8.8 | 代码格式化工具 |

### 3.2 后端技术栈

| 技术/框架 | 版本 | 说明 |
| :------- | :--- | :--- |
| Spring Boot | 3.0.6 | 应用开发框架 |
| Spring Cloud | 2022.0.2 | 微服务框架 |
| Spring Cloud Gateway | 4.0.6 | API网关 |
| Spring Security | 6.0.3 | 安全框架 |
| Spring Data JPA | 3.0.5 | 数据访问框架 |
| MyBatis-Plus | 3.5.3.1 | ORM框架 |
| MySQL | 8.0.33 | 关系型数据库 |
| Redis | 7.0.11 | 缓存数据库 |
| MongoDB | 6.0.5 | 文档型数据库，用于存储测试报告 |
| Elasticsearch | 8.7.1 | 搜索引擎，用于全文检索 |
| RabbitMQ | 3.11.15 | 消息队列 |
| Nacos | 2.2.1 | 服务注册与配置中心 |
| Sentinel | 1.8.6 | 流量控制和熔断降级 |
| Seata | 1.6.1 | 分布式事务 |
| Swagger/OpenAPI | 3.0.0 | API文档生成工具 |
| JUnit 5 | 5.9.3 | 单元测试框架 |
| Mockito | 5.3.1 | 测试模拟框架 |
| Logback | 1.4.7 | 日志框架 |
| Docker | 23.0.6 | 容器化平台 |
| Kubernetes | 1.27.1 | 容器编排平台 |

### 3.3 开发工具与环境

| 工具/环境 | 版本 | 说明 |
| :------- | :--- | :--- |
| IntelliJ IDEA | 2023.1 | Java开发IDE |
| Visual Studio Code | 1.77.3 | 前端开发IDE |
| Git | 2.40.1 | 版本控制系统 |
| Maven | 3.9.1 | 项目管理工具 |
| Node.js | 18.16.0 | JavaScript运行环境 |
| npm | 9.6.5 | 包管理工具 |
| Jenkins | 2.387.3 | 持续集成/持续部署工具 |
| SonarQube | 9.9.1 | 代码质量管理平台 |
| Nexus | 3.51.0 | 制品库 |
| Prometheus | 2.44.0 | 监控系统 |
| Grafana | 9.5.2 | 可视化监控工具 |

## 4. 数据库设计

### 4.1 数据库选型

系统采用多种数据库技术来满足不同的数据存储需求：

- **MySQL**：存储结构化数据，如用户信息、测试用例、测试计划等
- **Redis**：缓存频繁访问的数据，提高系统性能
- **MongoDB**：存储非结构化或半结构化数据，如测试报告、日志等
- **Elasticsearch**：提供全文检索功能，用于测试用例和缺陷的搜索

### 4.2 核心数据表设计

#### 4.2.1 用户与权限相关表

**用户表(t_user)**

| 字段名 | 类型 | 说明 |
| :----- | :--- | :--- |
| id | BIGINT | 主键，自增 |
| username | VARCHAR(50) | 用户名，唯一 |
| password | VARCHAR(100) | 密码（加密存储） |
| email | VARCHAR(100) | 邮箱 |
| phone | VARCHAR(20) | 手机号 |
| real_name | VARCHAR(50) | 真实姓名 |
| avatar | VARCHAR(255) | 头像URL |
| status | TINYINT | 状态：0-禁用，1-启用 |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |
| last_login_time | DATETIME | 最后登录时间 |

**角色表(t_role)**

| 字段名 | 类型 | 说明 |
| :----- | :--- | :--- |
| id | BIGINT | 主键，自增 |
| role_name | VARCHAR(50) | 角色名称 |
| role_code | VARCHAR(50) | 角色编码 |
| description | VARCHAR(255) | 角色描述 |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |

**用户角色关联表(t_user_role)**

| 字段名 | 类型 | 说明 |
| :----- | :--- | :--- |
| id | BIGINT | 主键，自增 |
| user_id | BIGINT | 用户ID，外键 |
| role_id | BIGINT | 角色ID，外键 |
| create_time | DATETIME | 创建时间 |

#### 4.2.2 用例库相关表

**用例库表(t_test_case_library)**

| 字段名 | 类型 | 说明 |
| :----- | :--- | :--- |
| id | BIGINT | 主键，自增 |
| library_name | VARCHAR(100) | 用例库名称 |
| library_code | VARCHAR(10) | 用例库编号 |
| description | TEXT | 描述 |
| is_public | TINYINT | 是否公开：0-私密，1-公开 |
| creator_id | BIGINT | 创建者ID，外键 |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |

**用例库成员表(t_library_member)**

| 字段名 | 类型 | 说明 |
| :----- | :--- | :--- |
| id | BIGINT | 主键，自增 |
| library_id | BIGINT | 用例库ID，外键 |
| user_id | BIGINT | 用户ID，外键 |
| role_type | TINYINT | 角色类型：1-管理员，2-编辑者，3-查看者 |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |

#### 4.2.3 测试用例相关表

**测试用例目录表(t_test_case_directory)**

| 字段名 | 类型 | 说明 |
| :----- | :--- | :--- |
| id | BIGINT | 主键，自增 |
| library_id | BIGINT | 所属用例库ID，外键 |
| parent_id | BIGINT | 父目录ID，自关联 |
| directory_name | VARCHAR(100) | 目录名称 |
| path | VARCHAR(500) | 目录路径 |
| sort_order | INT | 排序序号 |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |

**测试用例表(t_test_case)**

| 字段名 | 类型 | 说明 |
| :----- | :--- | :--- |
| id | BIGINT | 主键，自增 |
| case_title | VARCHAR(200) | 用例标题 |
| directory_id | BIGINT | 所属目录ID，外键 |
| library_id | BIGINT | 所属用例库ID，外键 |
| precondition | TEXT | 前置条件 |
| priority | TINYINT | 优先级：1-P0，2-P1，3-P2，4-P3 |
| case_type | TINYINT | 用例类型：1-功能测试，2-性能测试，3-安全测试等 |
| status | TINYINT | 状态：1-草稿，2-待评审，3-已评审 |
| creator_id | BIGINT | 创建者ID，外键 |
| assignee_id | BIGINT | 负责人ID，外键 |
| tags | VARCHAR(500) | 标签，逗号分隔 |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |

**测试步骤表(t_test_step)**

| 字段名 | 类型 | 说明 |
| :----- | :--- | :--- |
| id | BIGINT | 主键，自增 |
| case_id | BIGINT | 所属用例ID，外键 |
| step_order | INT | 步骤序号 |
| action | TEXT | 操作步骤 |
| expected_result | TEXT | 预期结果 |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |

#### 4.2.4 测试计划相关表

**测试计划表(t_test_plan)**

| 字段名 | 类型 | 说明 |
| :----- | :--- | :--- |
| id | BIGINT | 主键，自增 |
| plan_name | VARCHAR(100) | 计划名称 |
| project_id | BIGINT | 关联项目ID |
| iteration_id | BIGINT | 关联迭代ID |
| start_date | DATE | 开始日期 |
| end_date | DATE | 结束日期 |
| description | TEXT | 描述 |
| status | TINYINT | 状态：1-未开始，2-进行中，3-已完成 |
| creator_id | BIGINT | 创建者ID，外键 |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |

**计划用例表(t_plan_case)**

| 字段名 | 类型 | 说明 |
| :----- | :--- | :--- |
| id | BIGINT | 主键，自增 |
| plan_id | BIGINT | 测试计划ID，外键 |
| case_id | BIGINT | 测试用例ID，外键 |
| directory_id | BIGINT | 目录ID，外键 |
| execution_status | TINYINT | 执行状态：1-未执行，2-执行中，3-通过，4-失败，5-阻塞 |
| assignee_id | BIGINT | 执行人ID，外键 |
| execution_time | DATETIME | 执行时间 |
| actual_result | TEXT | 实际结果 |
| remark | TEXT | 备注 |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |

#### 4.2.5 缺陷相关表

**缺陷表(t_defect)**

| 字段名 | 类型 | 说明 |
| :----- | :--- | :--- |
| id | BIGINT | 主键，自增 |
| defect_title | VARCHAR(200) | 缺陷标题 |
| description | TEXT | 缺陷描述 |
| reproduction_steps | TEXT | 重现步骤 |
| plan_id | BIGINT | 关联测试计划ID，外键 |
| case_id | BIGINT | 关联测试用例ID，外键 |
| project_id | BIGINT | 关联项目ID |
| iteration_id | BIGINT | 关联迭代ID |
| severity | TINYINT | 严重程度：1-致命，2-严重，3-一般，4-轻微 |
| priority | TINYINT | 优先级：1-紧急，2-高，3-中，4-低 |
| status | TINYINT | 状态：1-待确认，2-处理中，3-已修复，4-已关闭，5-拒绝修复 |
| creator_id | BIGINT | 创建者ID，外键 |
| assignee_id | BIGINT | 处理人ID，外键 |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |
| close_time | DATETIME | 关闭时间 |

**缺陷附件表(t_defect_attachment)**

| 字段名 | 类型 | 说明 |
| :----- | :--- | :--- |
| id | BIGINT | 主键，自增 |
| defect_id | BIGINT | 缺陷ID，外键 |
| file_name | VARCHAR(100) | 文件名 |
| file_path | VARCHAR(255) | 文件路径 |
| file_size | BIGINT | 文件大小(字节) |
| file_type | VARCHAR(50) | 文件类型 |
| uploader_id | BIGINT | 上传者ID，外键 |
| create_time | DATETIME | 创建时间 |

**缺陷评论表(t_defect_comment)**

| 字段名 | 类型 | 说明 |
| :----- | :--- | :--- |
| id | BIGINT | 主键，自增 |
| defect_id | BIGINT | 缺陷ID，外键 |
| content | TEXT | 评论内容 |
| commenter_id | BIGINT | 评论者ID，外键 |
| create_time | DATETIME | 创建时间 |

#### 4.2.6 测试报告相关表

**测试报告表(t_test_report)**

| 字段名 | 类型 | 说明 |
| :----- | :--- | :--- |
| id | BIGINT | 主键，自增 |
| report_name | VARCHAR(100) | 报告名称 |
| report_type | TINYINT | 报告类型：1-计划报告，2-迭代报告，3-项目报告 |
| execution_start_date | DATE | 执行开始日期 |
| execution_end_date | DATE | 执行结束日期 |
| summary | TEXT | 总结 |
| template_id | BIGINT | 模板ID，外键 |
| creator_id | BIGINT | 创建者ID，外键 |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |

**报告计划关联表(t_report_plan)**

| 字段名 | 类型 | 说明 |
| :----- | :--- | :--- |
| id | BIGINT | 主键，自增 |
| report_id | BIGINT | 报告ID，外键 |
| plan_id | BIGINT | 计划ID，外键 |
| create_time | DATETIME | 创建时间 |

## 5. 接口设计

### 5.1 RESTful API设计规范

系统采用RESTful风格设计API，遵循以下规范：

1. **URI命名**：使用名词复数形式，如`/api/test-cases`
2. **HTTP方法**：
   - GET：获取资源
   - POST：创建资源
   - PUT：更新资源（全量更新）
   - PATCH：更新资源（部分更新）
   - DELETE：删除资源
3. **状态码**：
   - 200 OK：请求成功
   - 201 Created：资源创建成功
   - 204 No Content：请求成功但无返回内容
   - 400 Bad Request：请求参数错误
   - 401 Unauthorized：未认证
   - 403 Forbidden：无权限
   - 404 Not Found：资源不存在
   - 500 Internal Server Error：服务器错误
4. **响应格式**：统一使用JSON格式，包含状态码、消息和数据

### 5.2 核心API接口

#### 5.2.1 用户认证接口

```
POST /api/auth/login
POST /api/auth/logout
POST /api/auth/refresh-token
GET /api/auth/current-user
```

#### 5.2.2 用例库管理接口

```
GET /api/case-libraries
POST /api/case-libraries
GET /api/case-libraries/{id}
PUT /api/case-libraries/{id}
DELETE /api/case-libraries/{id}
GET /api/case-libraries/{id}/members
POST /api/case-libraries/{id}/members
DELETE /api/case-libraries/{id}/members/{userId}
```

#### 5.2.3 测试用例管理接口

```
GET /api/case-libraries/{libraryId}/directories
POST /api/case-libraries/{libraryId}/directories
PUT /api/case-libraries/{libraryId}/directories/{id}
DELETE /api/case-libraries/{libraryId}/directories/{id}

GET /api/test-cases
POST /api/test-cases
GET /api/test-cases/{id}
PUT /api/test-cases/{id}
DELETE /api/test-cases/{id}
POST /api/test-cases/batch-import
GET /api/test-cases/export
```

#### 5.2.4 测试计划管理接口

```
GET /api/test-plans
POST /api/test-plans
GET /api/test-plans/{id}
PUT /api/test-plans/{id}
DELETE /api/test-plans/{id}

GET /api/test-plans/{planId}/cases
POST /api/test-plans/{planId}/cases
PUT /api/test-plans/{planId}/cases/{caseId}
DELETE /api/test-plans/{planId}/cases/{caseId}
```

#### 5.2.5 缺陷管理接口

```
GET /api/defects
POST /api/defects
GET /api/defects/{id}
PUT /api/defects/{id}
DELETE /api/defects/{id}

POST /api/defects/{defectId}/attachments
DELETE /api/defects/{defectId}/attachments/{id}

GET /api/defects/{defectId}/comments
POST /api/defects/{defectId}/comments
DELETE /api/defects/{defectId}/comments/{id}
```

#### 5.2.6 测试报告接口

```
GET /api/test-reports
POST /api/test-reports
GET /api/test-reports/{id}
PUT /api/test-reports/{id}
DELETE /api/test-reports/{id}
GET /api/test-reports/{id}/export
```

## 6. 模块详细设计

### 6.1 用户认证与权限管理模块

#### 6.1.1 认证流程

系统采用JWT（JSON Web Token）实现无状态的用户认证机制：

1. 用户登录时，提交用户名和密码
2. 服务端验证用户名和密码，成功后生成JWT令牌
3. 客户端存储JWT令牌，并在后续请求中通过Authorization头部发送
4. 服务端验证JWT令牌的有效性和权限

#### 6.1.2 权限控制

系统采用RBAC（基于角色的访问控制）模型：

1. 用户关联角色，角色关联权限
2. 预定义角色：系统管理员、测试管理员、测试负责人、测试执行人员、开发人员、只读用户
3. 权限粒度：菜单权限、操作权限、数据权限
4. 实现方式：使用Spring Security的注解式权限控制和方法级别的权限检查

### 6.2 用例库管理模块

#### 6.2.1 用例库创建与配置

1. 支持创建公开和私密两种类型的用例库
2. 自动生成唯一的用例库编号
3. 配置用例库的基本信息和访问权限
4. 管理用例库成员及其权限

#### 6.2.2 用例库成员管理

1. 添加/移除用例库成员
2. 设置成员角色：管理员、编辑者、查看者
3. 权限继承：用例库的权限会影响其中的测试用例

### 6.3 测试用例管理模块

#### 6.3.1 用例目录管理

1. 支持多级目录结构
2. 目录的创建、编辑、删除和移动
3. 目录树的展示和导航

#### 6.3.2 测试用例创建与编辑

1. 手动创建测试用例，设置标题、前置条件、步骤、预期结果等
2. 批量导入测试用例，支持Excel和思维导图格式
3. 测试用例的编辑、复制、移动和删除
4. 测试步骤的添加、编辑和排序

#### 6.3.3 测试用例筛选与排序

1. 多条件筛选：执行者、状态、创建者、类型、优先级等
2. 快速筛选："只看我的"功能
3. 排序：按优先级、创建时间等排序
4. 搜索：全文搜索测试用例内容

### 6.4 测试计划管理模块

#### 6.4.1 测试计划创建与配置

1. 创建测试计划，设置名称、时间范围等
2. 关联项目和迭代
3. 配置测试计划的执行策略和工作流

#### 6.4.2 测试用例规划

1. 从用例库选择测试用例
2. 手动创建测试用例
3. 批量导入测试用例
4. 分配测试用例的执行人员

#### 6.4.3 测试执行

1. 执行测试用例，记录结果（通过、失败、阻塞等）
2. 记录实际结果和备注
3. 关联缺陷
4. 实时更新测试进度和通过率

#### 6.4.4 测试工作流配置

1. 自定义测试状态
2. 设置状态流转规则
3. 调整状态显示顺序

### 6.5 缺陷管理模块

#### 6.5.1 缺陷提交

1. 创建缺陷，设置标题、描述、重现步骤等
2. 设置严重程度和优先级
3. 上传附件（截图、日志等）
4. 关联测试用例和测试计划

#### 6.5.2 缺陷跟踪

1. 缺陷状态流转：待确认、处理中、已修复、已关闭等
2. 分配处理人
3. 添加评论和更新
4. 记录缺陷历史变更

### 6.6 测试报告模块

#### 6.6.1 报告创建与编辑

1. 基于测试计划创建报告
2. 设置报告名称、执行日期等
3. 添加总结性描述
4. 选择报告模板和风格

#### 6.6.2 报告内容生成

1. 自动生成测试统计信息
2. 测试用例执行情况统计
3. 缺陷分布统计
4. 高优先级和严重缺陷列表

#### 6.6.3 报告导出

1. 导出为PDF或图片格式
2. 支持在邮件或即时通讯工具中分享
